# Redirects for Netlify
# Handle API routes (if using serverless functions)
/api/* /.netlify/functions/:splat 200

# Handle Next.js static assets
/_next/* /_next/:splat 200

# SPA fallback - this should be last
/* /index.html 200
